<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
    <link
    href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css"
    rel="stylesheet"
    integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3"
    crossorigin="anonymous"
  />
    <link rel="stylesheet" href="style.css" />
  
  </head>
  <body>
    <header>
      <% if (user) { %>
        <nav>
          <ul class="nav nav-tabs">
   
            <li id="date" data-nextTime="<%= hasVoted.nexVote %>" data-hasVoted="<%= hasVoted.hasVoted %>" class="nav-item dropdown ">
              <a class="nav-link dropdown-toggle rounded-circle" data-bs-toggle="dropdown" href="#" role="button" aria-expanded="false">  <img src="<%= user.picture %>"  class="rounded-circle"></a></a>
              <ul class="dropdown-menu">
                <li><a id="email" class="dropdown-item" href="#"><%= user.email %></a></li>
                <li><a class="dropdown-item" href="/logout">Log out</a></li>
            
              </ul>
            </li>
            <li class="nav-item" role="presentation">
              <h4>Welcome <%= user.nickname %>   </h4>
              <span >you can vote Now</span>
              
            </li>
          
          </ul>
        </nav>
      
      
    <% }  else{  %>

    <a  class="btn btn-primary" href="/login"> LogIn or Sing Up to vote  </a>
  
    <% }    %>
        <h1>Vote For the Best Cartoon from the 1990's</h1>
      
      </header>
  
      <main>
        <section id="photos">
         
          <% for(let i=0; i < data.length; i++) {%>
            <section>
              <h3><%= data[i].name %></h3>
              <img src = "<%= data[i].url %>"  alt="<%= data[i].name %>" />
              <div class="votes">
                
                <% if ( !hasVoted.hasVoted && user   ) { %>
                  <form action="/<%= data[i].id %>"
                    method="POST"  
                  >
                  <button  data-id="<%= data[i].id %>" class="btn btn-primary" type="submit">Vote</button>
                </form>
                   
              <% } else  { %>
                <h5 class="votesUpdate"><%= data[i].votes %> out of <%= totalVotes %> votes</h5>
           
              <% } %>
              </div>
            </section>
         <% } %>
  
       <section>
      </main>
      <script src="https://cdn.jsdelivr.net/npm/moment@2.29.1/moment.min.js"></script>
 

      
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-ka7Sk0Gln4gmtz2MlQnikT1wXgYsOg+OMhuP+IlRH9sENBO0LRn5q+8nbTov4+1p" crossorigin="anonymous"></script>
      <script type="module"   src="/main.js"></script>
  </body>
</html>
